"""jwtauth - a small JWT authentication microservice.

Short-lived access tokens with rotating, hashed refresh tokens and
refresh-token reuse detection. In-memory stores make it a self-contained
portfolio/demo; swap the store for a real database in production.
"""

__version__ = "0.1.0"
